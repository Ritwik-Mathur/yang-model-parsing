server.port=8080
spring.application.name=yang-model-parser

# Allow large YANG file uploads (up to 10MB)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Logging
logging.level.com.yangparser=DEBUG
